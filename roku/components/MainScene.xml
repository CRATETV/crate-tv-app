<?xml version="1.0" encoding="UTF-8"?>
<component name="MainScene" extends="Scene">
    <children>
        <!-- Container where we’ll put HomeView -->
        <Group id="viewContainer" />

        <!-- Simple loading spinner (you can replace with an image) -->
        <Label
            id="spinner"
            text="Loading..."
            translation="[800, 500]"
            color="#FFFFFF"
        />

        <!-- Placeholder sidebar (you may already have a more complex one) -->
        <Group id="sideBar" translation="[50, 200]">
            <Label text="HOME" color="#FFFFFF" />
        </Group>

        <!-- Our Task that fetches content from cratetv.net -->
        <APITask id="apiTask" />
    </children>

    <script type="text/brightscript" uri="pkg:/components/MainScene.brs" />
</component>
