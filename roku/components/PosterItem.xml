<?xml version="1.0" encoding="utf-8" ?>
<component name="PosterItem" extends="Group">
    <interface>
        <field id="itemContent" type="node" onChange="onContentChanged" />
        <field id="width" type="float" />
        <field id="height" type="float" />
        <field id="rowFocusPercent" type="float" onChange="onFocusPercentChanged" />
    </interface>

    <script type="text/brightscript" uri="pkg:/components/PosterItem.brs" />

    <children>
        <!-- TOP 10 RANK NUMBER (Hidden by default) -->
        <LayoutGroup 
            id="rankGroup" 
            visible="false" 
            layoutDirection="horiz" 
            translation="[0, 60]">
            
            <!-- The Number Container -->
            <Rectangle width="120" height="200" color="#00000000">
                <!-- Hollow Number Effect (Text layered) -->
                <!-- Outline -->
                <Label 
                    id="rankOutline" 
                    text="" 
                    width="120"
                    horizAlign="right"
                    font="font:BoldSystemFont" 
                    size="130" 
                    color="#FFD700" 
                    translation="[-4, 0]" />
                <!-- Inner Black to create hollow look -->
                <Label 
                    id="rankFill" 
                    text="" 
                    width="120"
                    horizAlign="right"
                    font="font:BoldSystemFont" 
                    size="130" 
                    color="#141414" 
                    translation="[0, 0]" />
            </Rectangle>
        </LayoutGroup>

        <!-- MOVIE POSTER -->
        <!-- loadWidth/loadHeight are CRITICAL for performance -->
        <Poster 
            id="poster" 
            width="200" 
            height="300" 
            loadDisplayMode="scaleToZoom" 
            loadWidth="200" 
            loadHeight="300" />

        <!-- Focus Ring (Appears when selected) -->
        <Rectangle 
            id="focusRing" 
            width="210" 
            height="310" 
            translation="[-5, -5]" 
            color="#FFFFFF" 
            fill="false" 
            opacity="0" 
            stroke="4" />
    </children>
</component>